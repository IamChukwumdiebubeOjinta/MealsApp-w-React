const ChatBtn = ({styles}) =>(
    <div className={`w-[80px] h-[80px] box-shadow-sm flex items-center justify-center rounded-full bg-secondary text-white ${styles}`}>
        <b>i</b>
    </div>
)

export default ChatBtn